# 固件修改日志 (Change Log)

## 最新修改

### 1. 精简上报数据（JSON Payload 瘦身）
- **移除了 `analysis_config` 数据字段**：在生成上报服务器的 JSON 数据中，移除了 `analysis_config` 字段及相关的代码构造逻辑。
- **动态省略 `quality.attempts` 数组**：在自动量程（Auto Range）流程中，如果单次采集即成功（未发生削峰，无需现场重采调大量程），最终上报的 JSON 将彻底省略 `quality.attempts` 数组。服务器可通过外层的 `range_g` 以及特征中的峰值自行获知相关状态，从而进一步节省 4G 流量开销。只有在发生现场重采（即包含多条升级前后的测试记录）或达到最大量程仍削峰时，才会保留该数组供服务器进行比对。
- **修改 `quality.status` 为数字型**：为了方便后端判断，将原有的字符串状态改为整型数字：**`0` 代表采集正常 (`ok`)，`1` 代表削峰 (`clipped_at_max_range`)**。
- **示例文档更新**：同步修改了 `docs/report/result.json` 和 `docs/report/report.json` 的参考示例格式。

### 2. 优化自动量程策略（引入削峰容差）
- 优化了设备端判断波形削峰的敏锐度。由原先的“零容忍（哪怕只有1个点超过阈值就作废重采）”修改为“容差判定”。
- 新增了宏定义 `REPORT_CLIP_TOLERANCE_RATIO = 0.001f` (0.1%)。
- **效果**：如果单轴中达到削峰阈值的点数极少（比如 4096 个点中只有两三个毛刺点，比例 < 0.1%），固件会认为这是环境瞬态干扰，波形主体并未失真，因此**不算作削峰**。这段数据会被保留并上报，由云端算法根据 `clip_ratio` 进一步裁定。这不仅避免了设备端神经过敏地升档（导致分辨率下降），也减少了无效的现场重采。
